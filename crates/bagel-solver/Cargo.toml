[package]
name                   = "bagel-solver"
description            = "Bagel proof-of-work solver and challenge codec, built to wasm for the browser"
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
authors.workspace      = true

[lib]
crate-type = [ "rlib" ]

[lints]
workspace = true

[features]
host = [ "dep:vela" ]

[dependencies]
vela = { optional = true, workspace = true }
